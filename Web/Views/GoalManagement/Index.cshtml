@using Models
@using Web.Models
@model List<Goal>

@{
    ViewBag.Title = "Manage Goals";
}

@section breadcrumbs
{
    <ul class="breadcrumb">
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        <li class="active">Goals</li>
    </ul>
}

<div class="page-header">
    <h2>@ViewBag.Title</h2>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Name</th>
                <th>Category</th>
                <th>Goal Type</th>
                <th>Behaviour Type</th>
                <th>Change Value</th>
                <th>Unit Description</th>
                <th>Duration</th>
                <th>Actions</th>
            </tr>
        </thead>
        @foreach (Goal goal in Model)
        {
            <tr>
                @Html.HiddenFor(g => goal.Id)
                <td>@goal.Name</td>
                <td>@goal.Category.Name</td>
                <td>@goal.GoalType.GetDescription()</td>
                <td>@goal.BehaviourType.GetDescription()</td>
                <td>@goal.ChangeValue</td>
                <td>@goal.UnitDescription</td>
                <td>@goal.IntervalDuration.GetDescription()</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "GoalManagement", new { goalId = @goal.Id }, new { @class = "btn btn-primary btn-xs" })
                    @Html.ActionLink("Delete", "Delete", "GoalManagement", new { goalId = @goal.Id }, new { @class = "btn btn-danger btn-xs" })
                </td>
            </tr>
        }
    </table>
</div>

<div>
    @Html.ActionLink("Create Goal", "CreateGoal", "GoalManagement", null, new { @class = "btn btn-primary btn-lg" })
</div>
